CREATE ALGORITHM=UNDEFINED DEFINER=`root`@`localhost` SQL SECURITY DEFINER VIEW `d_hr`.`employee_count_per_manager` AS select `d_hr`.`records_of_managers_per_employee`.`manager` AS `manager`,count(0) AS `employee_count` from `d_hr`.`records_of_managers_per_employee` where (`d_hr`.`records_of_managers_per_employee`.`manager` is not null) group by `d_hr`.`records_of_managers_per_employee`.`manager`